apiVersion: triggers.tekton.dev/v1beta1
kind: TriggerTemplate
metadata:
  name: mycolorforge-backend-template
  namespace: tekton-pipelines
  labels:
    app.kubernetes.io/part-of: mycolorforge
spec:
  params:
  - description: The git revision (branch/tag/sha)
    name: git-revision
  - description: The git repository URL
    name: git-url
  - description: The image tag to use
    name: image-tag
  - name: pipeline-mode
    default: full
  resourcetemplates:
  - apiVersion: tekton.dev/v1
    kind: PipelineRun
    metadata:
      generateName: mycolorforge-backend-
      labels:
        app.kubernetes.io/part-of: mycolorforge
        tekton.dev/pipeline: mycolorforge-backend-pipeline
      namespace: tekton-pipelines
    spec:
      params:
      - name: git-url
        value: $(tt.params.git-url)
      - name: git-revision
        value: $(tt.params.git-revision)
      - name: image-tag
        value: $(tt.params.image-tag)
      - name: security-scan-enabled
        value: 'true'
      - name: sonar-enabled
        value: 'true'
      - name: cosign-enabled
        value: 'true'
      - name: sbom-enabled
        value: 'true'
      - name: pipeline-mode
        value: $(tt.params.pipeline-mode)
      pipelineRef:
        name: mycolorforge-backend-pipeline
      workspaces:
      - name: shared-workspace
        volumeClaimTemplate:
          spec:
            accessModes:
            - ReadWriteOnce
            resources:
              requests:
                storage: 2Gi
            storageClassName: local-path
      - emptyDir: {}
        name: go-cache
      - emptyDir: {}
        name: trivy-cache
      - name: docker-credentials
        secret:
          secretName: harbor-credentials
      - name: git-credentials
        secret:
          secretName: github-credentials
      - name: sonar-credentials
        secret:
          secretName: sonar-credentials
      - name: cosign-key
        secret:
          secretName: cosign-key
